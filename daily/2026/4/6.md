## 2026-04-06

### 本日のテーマ

* Linux
  * ファイルシステム
    * ディレクトリ構造

※ なんとなくわかった気でいたディレクトリ構造を学ぶ

### 学んだこと

#### /etc

設定ファイルが格納される

/etc/ssh
/etc/systemd

#### /usr

ユーザープログラムの本体です

/usr/bin
/usr/sbin
/usr/lib
/usr/local # 手動インストールが入りがち

#### /dev

これはデバイスファイル

/dev/sda
/dev/nvme0n1
/dev/null
/dev/zero
/dev/random
/dev/tty

```
lsblk

ディスクの構造を把握するコマンド
```

EC2 の EBS
マウント前のデバイス確認
Docker のデバイスマウント
コンテナ権限問題

####　/proc

プロセスの情報とカーネル情報の仮想ファイルシステム


cat /proc/cpuinfo # CPU 情報
cat /proc/meminfo # メモリ 情報
cat /proc/sys/kernel/hostname

ls -l /proc/$$/fd # 現在のシェルで開いているファイル


```
sudo ls /proc/1/ns
cgroup	ipc  mnt  net  pid  pid_for_children  time  time_for_children  user  uts

 sudo ls /proc/1
arch_status  clear_refs		 cpuset   fdinfo	     latency	mem	    ns		   pagemap	sched	   smaps_rollup  syscall	 uid_map
attr	     cmdline		 cwd	  gid_map	     limits	mountinfo   numa_maps	   patch_state	schedstat  stack	 task		 wchan
autogroup    comm		 environ  io		     loginuid	mounts	    oom_adj	   personality	sessionid  stat		 timens_offsets
auxv	     coredump_filter	 exe	  ksm_merging_pages  map_files	mountstats  oom_score	   projid_map	setgroups  statm	 timers
cgroup	     cpu_resctrl_groups  fd	  ksm_stat	     maps	net	    oom_score_adj  root		smaps	   status	 timerslack_ns
```

#### /sys

カーネル・デバイスの状態管理

デバイス情報
カーネル設定
cgroup 系
ストレージやネットワークの属性


### /run

起動中のランタイム状態

```
ls /run
NetworkManager  containerd    docker       initramfs  openvpn               runc               snapd.socket       tmpfiles.d                    user
alsa            credentials   docker.pid   lock       openvpn-client        sendsigs.omit.d    speech-dispatcher  ubuntu-advantage              utmp
avahi-daemon    crond.pid     docker.sock  log        openvpn-server        setrans            spice-vdagentd     udev                          uuidd
blkid           crond.reboot  gdm3         motd.d     plymouth              shm                sudo               udisks2                       wpa_supplicant
cloud-init      cups          gdm3.pid     mount      reboot-required       snapd              systemd            unattended-upgrades.lock
console-setup   dbus          initctl      netns      reboot-required.pkgs  snapd-snap.socket  thermald           unattended-upgrades.progress
```

### /mnt と /media

/mnt は手動マウント

/media はリムバブルメディア向け